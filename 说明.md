杂牌云 / VPS 多云自动化 —— 一张完整蓝图
────────────────────

# 杂牌云 / VPS 多云自动化系统

## 项目概述
本项目实现了一套完整的多云VPS自动化管理系统，用于搭建分布式的代理服务网络。主要特点包括：

- 支持30-50台VPS分布在多个云服务商
- 支持VMess/VLESS/Trojan/Reality等协议
- 快速部署能力：5分钟拉起新节点，10分钟完成替补
- 集中化的用户/流量管理、内网自愈和指标监控

## 系统架构

```
       ┌──────────┐ ①开机+cloud-init    ┌──────────────────────────┐
多云→  │ VPS 批量 │ ──────────────────▶ │ bootstrap(setup.sh)      │
       └──────────┘                    │• 安装 Docker & Xray       │
                                        │• 加入 Netmaker Mesh      │
                                        └─────────┬───────────────┘
                                                  │
                                                  │WireGuard 内网
                                                  ▼
┌──────────────┐ Heartbeat/流量  ┌──────────────────────┐
│ Xray 节点群  │───────────────▶│ V2Board / X-UI 面板 │②服务层控制
└──────────────┘                └─────────┬────────────┘
                                          │
                      Prom scrape         ▼
                   ┌────────────────────────────┐
                   │ Prometheus + Loki + Grafana │③观测
                   └────────────────────────────┘
```

## 组件与职责

| 层次 | 组件 | 作用 |
|------|------|------|
| 网络 | Netmaker | WireGuard Mesh，节点注册 / IPAM / ACL |
| 服务 | Xray 容器 | 提供翻墙协议 (VMess/VLESS/Trojan/Reality) |
| 控制 | V2Board 或 X-UI | 账号、订阅、流量统计、节点心跳 |
| 自动化 | cloud-init + setup.sh | 节点自举；30 s 内完成安装并注册 |
| 配置 | ansible-pull + GitOps | 15 min 周期拉取 Git 更新 (config / cert) |
| 监控 | Prometheus / Grafana / Alertmanager | CPU、带宽、TLS 成功率、告警 |
| 证书 | acme.sh DNS-01 | 自动签发 / 续期，USR1 热加载到 Xray |
| CI/CD | GitHub Actions | terraform fmt/plan、playbook 检查、lint |

## 项目结构

```
vpnhello/
├─ packer/           # 通用 QCOW2 基础镜像脚本
│   └─ template.json # Packer模板
├─ cloud-init/       # 云初始化配置
│   └─ base-cloudinit.yml # 基础云初始化配置
├─ scripts/
│   ├─ setup.sh      # 一键安装 Xray + Netmaker + exporter
│   ├─ replace-node.sh # 节点替换脚本
│   ├─ check-nodes.sh  # 节点状态检查脚本
│   └─ monitoring/   # 监控组件配置
├─ ansible/
│   ├─ inventory/hosts.yml # Ansible主机清单
│   ├─ playbooks/site.yml  # 主playbook
│   └─ roles/       # Ansible角色
│       ├─ xray/    # Xray服务配置
│       ├─ tls/     # TLS证书管理
│       └─ monitor/ # 监控配置
├─ v2board/         # V2Board面板配置
│   └─ docker-compose.yml
├─ x-ui/            # X-UI面板配置（替代方案）
│   └─ docker-compose.yml
├─ netmaker/        # Netmaker网络配置
│   ├─ docker-compose.yml
│   └─ Caddyfile
├─ terraform/       # 基础设施即代码
│   ├─ modules/     # Terraform模块
│   └─ environments/ # 环境配置
└─ .github/workflows/ci-cd.yml # CI/CD配置
```

## 全生命周期流程

1. **VPS初始化**：
   - 在VPS控制台克隆基础镜像
   - 注入 base-cloudinit.yml（若无 user-data 就手工 curl | bash）
   - 开机 30s 内完成 setup.sh 安装

2. **节点自举**：
   - 安装 Docker / Xray / node_exporter
   - netclient join → Netmaker 自动分配内网 10.x.x.x
   - 上报面板 API，领取节点 ID & 初始 config.json

3. **运行与监控**：
   - 1 min 内节点可用，Prometheus 自动发现并开始监控
   - 开发者 push 修改 (流量策略 / TLS 域名) → GitHub Actions 触发 ansible-playbook → 所有节点热更新
   - 证书到期前 30 天：acme.sh DNS-01 自动续期 → docker kill -s USR1 xray 热加载

4. **故障处理**：
   - 节点失联 > 5 min，Alertmanager 告警
   - 使用 replace-node.sh 脚本销毁旧 VPS，再按步骤 1 新开一台

## 快速开始

### 1. 控制节点设置

1. 克隆本仓库：
   ```bash
   git clone https://github.com/yourusername/vpnhello.git
   cd vpnhello
   ```

2. 设置Netmaker：
   ```bash
   cd netmaker
   # 编辑 docker-compose.yml 和 Caddyfile 配置域名和密钥
   docker-compose up -d
   ```

3. 设置控制面板（二选一）：
   ```bash
   # V2Board
   cd ../v2board
   # 编辑 docker-compose.yml 配置
   docker-compose up -d
   
   # 或者 X-UI
   cd ../x-ui
   # 编辑 docker-compose.yml 配置
   docker-compose up -d
   ```

4. 设置监控系统：
   ```bash
   cd ../scripts/monitoring
   # 编辑 prometheus/prometheus.yml 和其他配置
   docker-compose up -d
   ```

### 2. 节点部署

1. 配置Terraform变量：
   ```bash
   cd ../terraform/environments/prod
   cp terraform.tfvars.example terraform.tfvars
   # 编辑 terraform.tfvars 填入云服务商API密钥
   ```

2. 部署基础设施：
   ```bash
   terraform init
   terraform apply
   ```

3. 配置Ansible：
   ```bash
   cd ../../ansible
   # 编辑 inventory/hosts.yml 或使用terraform生成的inventory
   ansible-playbook -i inventory/hosts.yml playbooks/site.yml
   ```

### 3. 节点管理

- 检查节点状态：
  ```bash
  ../scripts/check-nodes.sh
  ```

- 替换故障节点：
  ```bash
  ../scripts/replace-node.sh <provider> <node_id>
  ```

## 监控与告警

- Grafana面板：http://your-monitor-server:3000
- Prometheus：http://your-monitor-server:9090
- Alertmanager：http://your-monitor-server:9093

## 安全注意事项

- 所有API密钥和凭证应妥善保管，不要提交到Git仓库
- 使用强密码和SSH密钥认证
- 定期更新系统和组件
- 监控异常流量和连接

## 贡献与开发

欢迎提交Issue和Pull Request。在开发新功能前，请先创建Issue讨论。

## 许可证

MIT

## 免责声明

本项目仅供学习和研究使用，请遵守当地法律法规。使用者需自行承担使用本项目的风险和责任。



